{% extends 'base.html.twig' %}

{% block title %}Hello !{% endblock %}

{% block body %}

    <h3>{{ articles.owner.name }}</h3>
    {% for article in articles %}
        <h5 class="ml-5">{{ article.title }}<a href="{{ path('article_show', {'id':article.id}) }}"> Show</a></h5>
    {% endfor %}

{% endblock %}
